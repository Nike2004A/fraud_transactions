# Fraud transactions capstone - deps pinned (Fase 3)
# Python 3.11 (pyproject.toml: requires-python = ">=3.11,<3.12")
#
# Crear venv:
#   python3.11 -m venv .venv
#   source .venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#   pip install -e .          # instala el paquete src/ en modo editable
#
# Notas CUDA:
# - xgboost>=2.0 desde PyPI ya incluye soporte CUDA cuando se usa device='cuda'.
#   No requiere build especial. Necesita driver NVIDIA (detectado: 565.90 / RTX 4060).

# --- Core ML ---
xgboost>=2.0,<3.0
scikit-learn>=1.4,<2.0
numpy>=1.26,<3.0
pandas>=2.0,<3.0

# --- IO ---
pyarrow>=15
duckdb>=0.10
polars>=0.20

# --- Tracking & HPO ---
mlflow>=2.10,<3.0
optuna>=3.5,<5.0
optuna-integration[xgboost]>=3.5

# --- Explainability (Fase 4) ---
shap>=0.44

# --- Plots ---
matplotlib>=3.8
seaborn>=0.13

# --- Utils ---
joblib>=1.3

# --- Dev ---
pytest>=8.0
pytest-cov>=4.1
ruff>=0.4
ipykernel>=6.29
jupyterlab>=4.1
